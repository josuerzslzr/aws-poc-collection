# spring-aws-dynamo

This is a simple Spring Boot application that hosts a Rest API that handles persistence of a Customer entity in **Dynamo DB**

`src`: Java sources
`scripts`: Shell scripts to publish to AWS
